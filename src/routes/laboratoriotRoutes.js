const { Router } = require('express')
const router = Router()


// C: Crear un nuevo laboratorio
router.post('/', );

// R: Obtener todos los laboratorios 
router.get('/', );

// R: Obtener un laboratorio por su ID 
router.get('/:idLaboratorio', );